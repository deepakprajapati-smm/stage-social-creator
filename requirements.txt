# STAGE Social Creator — Python dependencies
# Install: pip install -r requirements.txt
# Then:    patchright install chrome

# ── Web automation ────────────────────────────────────────────────────────────
patchright>=1.0.0          # Stealth Playwright fork (FB + YT via Chrome CDP)
# playwright>=1.40.0       # Fallback if patchright not available

# ── Mobile automation (Instagram via GeeLark) ─────────────────────────────────
Appium-Python-Client>=3.0.0  # Appium client for GeeLark cloud phones

# ── API ───────────────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0

# ── HTTP ──────────────────────────────────────────────────────────────────────
requests>=2.31.0

# ── Transliteration (Hindi → social handles) ─────────────────────────────────
indic-transliteration>=2.3.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0        # For loading .env file (optional)
